name: Process Data

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  process-data:
    runs-on: ubuntu-latest

    steps:
    # Rest of your workflow...
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy py7zr matplotlib seaborn scikit-learn
        
    - name: Create directories
      run: |
        mkdir -p data/processed
        
    - name: Run data processing script
      run: |
        python data/processed/clean_data.py
        
    - name: Commit processed data
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/processed/cleaned_stock_data.csv
        git commit -m "Add processed data" || echo "No changes to commit"
        git push
